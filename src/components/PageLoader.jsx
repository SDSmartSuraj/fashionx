function PageLoader() {
  return (
    <div className="fixed inset-0 bg-black flex items-center justify-center z-[9999]">
      <h1 className="text-white text-6xl font-black tracking-[10px] animate-pulse">
        FASHIONX
      </h1>
    </div>
  );
}

export default PageLoader;